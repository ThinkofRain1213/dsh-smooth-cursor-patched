# dsh-smooth-cursor-patched — DSH installable bundle patch
# Registers the patched plugin row into the web profile's client roster.
- insert:
    - id: smooth-cursor-patched
      name: dsh-smooth-cursor-patched
